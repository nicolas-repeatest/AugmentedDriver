package augmenteddriver.web.pageobjects;

import augmenteddriver.web.AugmentedWebDriver;

/**
 * Generic interface for executing an action.
 */
public interface Action {
    void execute(AugmentedWebDriver driver);
}
